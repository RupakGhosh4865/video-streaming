const request = require('supertest');
const express = require('express');

describe('Video Endpoints Tests', () => {
    it('should have basic tests configured properly', () => {
         expect(true).toBe(true);
    });
});
